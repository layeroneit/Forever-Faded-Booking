# Frontend (Vite) — used by the Book page for Stripe Elements.
# Copy to .env and fill in. Do NOT commit .env or put real keys here.
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxxx

# Backend (Lambda) — set STRIPE_SECRET_KEY in Amplify, not in this file.
# In Amplify Console: App → Environment variables → Add STRIPE_SECRET_KEY (or use Secrets).
# For local sandbox: configure via Amplify secrets or your CI/deploy pipeline.
